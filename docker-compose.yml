version: '3.8'

services:
  ai-governance:
    build: .
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"  # For future web interface
    restart: unless-stopped